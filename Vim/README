Installation
------------
1. Create symbolic links:
    ln -s `pwd`/_vimrc $HOME/.vimrc
    ln -s `pwd`/_gvimrc $HOME/.gvimrc

2. Install the addon manager:
    sh InstallAddonManager.sh

3. Start Vim:
    vim

4. Wait while the plugins are installing.

5. Done.

